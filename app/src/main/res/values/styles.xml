<resources>

    <style name="gameBtn" parent="android:style/Widget.Button">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/game_text_light</item>
        <item name="android:background">@color/game_button</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:textColorPrimary">@color/game_text_dark</item>
        <item name="android:buttonStyle">@style/gameBtn</item>

    </style>

</resources>
